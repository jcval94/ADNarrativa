# Notation Contract v0_1

Compact notation is derived from validated JSON:

```text
(FUNCIONES)[CERTEZA]_EMOTIONINTENSITY{STANCE}
```

Examples:

```text
(P+V)_S1{0}
(K+Y)!_E2{-}
(S+I+U)_C1{+}
```

The compiler is the only component allowed to produce `final_notation`.
